<!-- Maintainer note, stripped before this file enters Claude's context.

     The instructions themselves live in AGENTS.md at the repo root, imported
     below, so that Claude Code and other coding agents read one file rather
     than drifting copies — Copilot's coding agent reads AGENTS.md natively.

     Deeper reference material is in .claude/skills/. Those load on demand
     rather than at launch, which is the point: @-imports are expanded into
     context at startup, so splitting into imports would organise this file
     without reducing what it costs every session.

     Keep AGENTS.md to rules that must apply before anything is read. If a rule
     only matters while doing one kind of work, it belongs in a skill. -->

@../AGENTS.md
